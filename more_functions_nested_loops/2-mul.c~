/**
* mul - multiplies two integers.
* @a: first intergers
* @b: 2nd intergers
* Return: Always mul (Success)
*/

#include "main.h"
#include <stdio.h>

int mul(int a, int b)
{
printf("%d\n", a * b);
return (a * b);
}
