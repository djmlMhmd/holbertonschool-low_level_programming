#include "main.h"
#include <stdio.h>

/**
 * _islower - checks for lowcase charac
 * @c : the charac to be check
 * Return: 1 for lowcase chara or 0 for other
 */

int _islower(int c)
{
if (c >= 'a'  && c <= 'z')

return (1);

else

return (0);
}
