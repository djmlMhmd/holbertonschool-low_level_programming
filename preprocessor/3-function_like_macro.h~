#include <stdio.h>



/**
 * main - Entry point
 *
 * Return: Always 0
 */
#define ABS(x) ((x) < 0 ? -(x) : (x))

int main(void)
{
int x = -10;
int y = ABS(x);
printf("The absolute value of %d is %d\n", x, y);
return (0);
}
